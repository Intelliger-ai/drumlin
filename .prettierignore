node_modules/
dist/
pnpm-lock.yaml
*.tgz

# Test fixtures are inputs, not source.
#
# Several rules read source text rather than only the AST — confirmation
# markers, pending copy, disabled states — so reformatting a fixture can change
# what the analysis finds. A formatter is exactly the kind of change that would
# do it silently, and the resulting test failure would look like a rule bug.
packages/indexer/fixtures/

# Generated by the build, and copies of files that live at the root.
packages/drumlin/dist/
packages/drumlin/README.md
packages/drumlin/LICENSE

# Not in the repository; listed so a local checkout that has them is left
# alone. See the note in .gitignore.
vault/
Context/
